/**
 * 排序方向枚举
 *
 * @author dongjak
 * @since 1.0
 * @date 2023/10/7
 */
export enum SortDirection {
    /**
     * 升序，用于表示结果应该从小到大排序
     */
    ASC = "asc",

    /**
     * 降序，用于表示结果应该从大到小排序
     */
    DESC = "desc"
}
